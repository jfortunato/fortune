default:
    suites:
        resource:
            contexts: [ ResourceContext, ApiContext ]
    gherkin:
        filters:
            tags: ~@wip&&~@skip&&~doctrine
    formatters:
        progress: ~
